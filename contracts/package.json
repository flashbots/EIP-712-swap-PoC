{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.2",
    "hardhat": "^2.9.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.6.0",
    "@uniswap/v3-periphery": "^1.4.1",
    "dotenv": "^16.0.0"
  },
  "scripts": {
    "copy-abi": "ABI=$(cat artifacts/contracts/SonOfASwap.sol/SonOfASwap.json | jq .abi) && echo $ABI > ../backend/src/abi/sonOfASwap.json && echo $ABI > ../frontend/src/sonOfASwap.json",
    "deploy-goerli": "npx hardhat run --network goerli scripts/deploy.js && yarn copy-abi",
    "deploy-local": "npx hardhat run --network local scripts/deploy.js && yarn copy-abi",
    "fake-deploy": "npx hardhat run scripts/deploy.js && yarn copy-abi"
  }
}
